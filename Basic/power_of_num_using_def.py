import math

base = int(input("Enter the Base value:-"))
exponent = int(input("Enter the Base exponent:-"))

power = math.pow(base, exponent)

print(base, "raised to the power of", exponent, "is", power)